/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.dummyjson.com',
            'img.freepik.com'
        ],
    },
};

export default nextConfig;
